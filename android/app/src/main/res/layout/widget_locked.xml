<?xml version="1.0" encoding="utf-8"?>
<!-- Shown whenever Noma is locked (or the lock state is unknown): fail closed. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="#E9E9EE">

    <ImageView
        android:id="@+id/widget_lock_icon"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:src="@drawable/widget_ic_lock"
        android:alpha="0.8" />

    <TextView
        android:id="@+id/widget_locked_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Noma locked"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="#3A3A3C" />

    <TextView
        android:id="@+id/widget_locked_sub"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="Tap to unlock"
        android:textSize="12sp"
        android:textColor="#6E6E73" />

</LinearLayout>
